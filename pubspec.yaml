name: roulette
description: A flutter package provide a roulette widget.
version: 0.0.1
repository: https://github.com/do9core/roulette

environment:
  sdk: ">=2.15.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:

